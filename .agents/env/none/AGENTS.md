## Environment

None. Harness only. Docker, Kubernetes, cluster tooling NOT available — do not
assume any and do not install one to route around this.

Need an environment? Ask human first. Then:

```bash
./joharness.sh env          # what layers exist
./joharness.sh env k8s      # select one
```
