joharness — notices

This file travels with the harness. It is copied into every consumer
repository by the sync engine (.agents/scripts/sync-to-consumer.sh) together
with LICENSE beside it, so that the grant reaches every copy, which is the
one condition the MIT License sets.

Grant

The harness is licensed under the MIT License. The license text and its
copyright notice are in LICENSE beside this file, a byte-identical copy of
the canonical repository's root LICENSE:

  Copyright (c) 2026 Christian Westhoff and joharness contributors

In a consumer repository the grant covers the files that came from the
canonical repository and nothing else. Which files those are is decided by
the tools that place them, not by a path list here: the sync engine's
FILES and DIRS in .agents/scripts/sync-to-consumer.sh (joharness.sh,
CLAUDE.md, root AGENTS.md above its "# Part 2 — project" marker,
.gitattributes, the .claude/ settings, commands, skills and agents, the
.agents/docs/ and .agents/harness/ trees minus the canonical-only selftest,
.agents/env/README.md plus the one environment layer the consumer selects,
and this file with LICENSE), and the files .agents/scripts/bootstrap-consumer.sh
seeds once (its ci.yml, update.yml, README and AGENTS.md Part 2 stubs, which
are the consumer's to edit from then on). A file the consumer wrote under
any of those paths — its own environment layer under .agents/env/, its own
skill under .claude/skills/ — is the consumer's, and so is everything
outside them: the root LICENSE, AGENTS.md below the marker, docs/, the
repository's own code.

Third-party material

The MIT License text in LICENSE beside this file applies to each item below
together with the copyright line given for it.

- .agents/docs/caveman.md is distilled from the caveman skill
  (https://github.com/JuliusBrussee/caveman): rules, the pattern line and
  the Not/Yes example from skills/caveman/SKILL.md, reproduced or closely
  paraphrased, and the motto from its README.md. MIT License,
  Copyright (c) 2026 Julius Brussee. That file reproduces the full notice
  itself, in a closing section, so a copy of it made on its own is complete.

- .agents/docs/graph.md and .agents/docs/plans/README.md adapt task-graph
  rules from Graph Engineering
  (https://github.com/codejunkie99/graph-engineering). Ideas, cited at the
  point of use; no text is reproduced. MIT License,
  Copyright (c) 2026 codejunkie99.

- .agents/docs/orchestrated.md adapts role, health and dispatch ideas from
  Gas Town (https://github.com/gastownhall/gastown, read at commit 649b832).
  Ideas, cited at the point of use; no text is reproduced. MIT License,
  Copyright (c) 2025 Steve Yegge.

Short quotations elsewhere in .agents/docs/ (Vale's documentation, Martin
Fowler's Bounded Context page, arXiv abstracts) are attributed with a link
where they appear and carry no license condition into this repository.

Not distributed here

Environment layers under .agents/env/ may download tools at provisioning
time. Those are fetched from their own sources under their own licenses and
are not part of this repository.
