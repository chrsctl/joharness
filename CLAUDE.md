<!-- Pure pointer. Claude Code loads THIS file, not AGENTS.md, and harnesses
     that read AGENTS.md natively resolve no imports — so content here is a
     second copy that reaches one reader and not the other.
     (.agents/docs/handover/README.md, How a session finds this
     without being told.) -->
<!-- Instruction text goes in AGENTS.md, never here. -->

@AGENTS.md
