---
workstream: janitor-2026-09-17
status: in-progress
branch: claude/drain-8jr601
pr: none
plan: none
issue: none
session: https://claude.ai/code/session_01K6sHM4RWyYDCLZmrDSrWk3
agent: opus
updated: 2026-09-17
next: Retire this file in the last commit before the pull request, then merge
---

## Goal

`./joharness.sh drain` and `./joharness.sh janitor` both read `janitor : DUE`
— 648h since the repository began, no sweep having landed. First sweep this
repo has had. Seven candidates, five of them holding a plan out of the queue
while their claims stand.

A claim outlives the session that made it and nothing releases it. The role
exists because of two measurements in a consumer: issue #254, one unowned
block held four plans out of the queue for 141 hours while every pass printed
it as `holds no slot`; issue #249, the mechanism that answers liveness is the
one nobody schedules.

## Decisions

- **Push age names a candidate; only the control plane names a verdict.** The
  command's own output says liveness is not in it. Every release below cites
  the reading that proved it, per the field table in
  `.claude/commands/orchestrate.md` step 2 — `ARCHIVED` or not found, or a
  FAILED bucket confirmed by a second read with `updated_at` and the head
  both unchanged. `IDLE` alone is between turns, not gone, whatever the push
  age says.
- **Two reads for anything that is not ARCHIVED or absent.** The cost of the
  safe direction is one sweep's delay; the cost of the other is destroyed
  work in progress and a duplicate session on a live branch.

## Rejected

- **Releasing the claim that names a pull request.**
  `claude/multi-agent-orchestration-pr-jyli0w` reads ARCHIVED like the other
  six, and the role's rule is explicit: "A candidate naming a `pr:` is not
  yours even when its session is gone." Left alone and reported. Recorded
  because the rule's PREMISE is false here — see r2 — and the next session to
  read this should know the exemption was applied on the letter, not because
  the work is nearly done.
- **Fixing the defect in r1.** It is in `joharness.sh`, which is protocol
  text, and the role says it touches no code. Filed as #278 instead, with the
  table and both counted queue readings.

## Review

- r1: (session) **the command's `holds:` line is false, on 5 of 5 candidates
  that name a plan, and it is the role's whole justification.** It prints
  `docs/plans/<x>.md, out of the queue while this claim stands` from the
  claim's own `plan:` field, with no check that the plan is on the base
  branch. All five named plans exist only on their own unmerged branch:
  `git ls-tree --name-only origin/main docs/plans/` returns four files and
  none of them is one of the five. So the claims held nothing out of the
  queue, and `drain` read `4 free plan(s)` before this sweep and `4 free
  plan(s)` after all six releases — the number the line predicts would move
  did not, because it could not. `joharness.sh:5337`. (wontfix here — it is
  protocol text and this role touches no code. Filed as #278 with the table,
  the code line and both queue readings. What the sweep DID buy is smaller
  and real: six claims the handover hook presented as live work now read
  `status: abandoned`, so no session spends a `/who` on them.)
- r3: (verifier) **writing `status: abandoned` reddens `ci` on the branch it
  releases, every time, by default.** Each released branch carries its own
  older `joharness.sh`, whose enum is `lint_enum "$rel" status "$val"
  in-progress blocked review "done"` — no `abandoned` — so the release makes
  that branch's own graph lint print `DEAD ...: status 'abandoned' not one
  of: ...` and `ci: FAIL`. Re-checked here on all six: every copy carries the
  four-value enum. `abandoned` entered main's enum in `ce504c9` at
  2026-09-17 17:30:09 +0000, HOURS before this sweep, so every branch in the
  repository is older than the word. A returning session running step 5's
  `ci` before touching anything gets a red it did not write. (wontfix here —
  the remedy is in `joharness.sh`, protocol text this role may not touch, and
  nothing on my own branch changes it. Filed as #279 with the enum dump and
  the commit that introduced the word. The mitigation that already exists and
  is unstated: those branches are 1300+ behind and must reconcile with main
  to merge at all, which brings the new enum.)
- r4: (verifier) **`./joharness.sh graph` still shows all six abandoned
  claims claiming their plans.** `queue-context.sh` filters `$3 !=
  "abandoned"` at lines 488 and 583; `cmd_graph` contains the word zero
  times. Verified post-sweep: six live `claims` edges for six released
  claims. Two readers of one fact disagree, which `graph.md`'s one-derivation
  premise forbids. (wontfix here — same reason, filed in #279.)
- r5: (verifier) **the hook's row renders `claims issue #230` on an abandoned
  claim while its own summary reports the issue free.** Verified in the same
  output: the row says it, and `Issues claimed by work in flight` lists none.
  `handover-context.sh:604` excludes abandoned from the issue set; the row
  label at `:664` does not. (wontfix here — filed in #279.)
- r6: (verifier) **recording abandonment erased the only git-derived signal
  FOR abandonment.** All six carried the hook's `STALE: pushed N weeks ago —
  read as abandoned from git alone` marker; after the release all six read
  `pushed 29 minutes ago  <- recent`, because a release is a push. Verified.
  It also drops them out of `janitor`'s own age window for six days on age
  grounds alone. (wontfix here — filed in #279, where it is the one of the
  four that may be acceptable as designed.)
- r7: (verifier) **the release note asserted something its own commit
  falsified, on all six branches**: "nothing was deleted — not a line of what
  the claim already said", in a commit that deletes the old `next:` line and,
  on four files, `None.` under `## Blockers`. (fixed — one corrective commit
  per branch; the note now names exactly the frontmatter lines a release
  replaces, and says the earlier wording was contradicted by its own commit.)
- r8: (verifier) **one released file had INVALID YAML frontmatter** — my
  `next:` on `unsupervised-endurance.md` contained an unquoted `: ` ("still
  the human's: a heartbeat Routine"). Re-checked with `yaml.safe_load` across
  all seven files at both commits: that one file, only after my release, and
  it parsed cleanly before. The harness's own readers are awk-based and
  tolerant, so nothing here broke; the protocol calls the block YAML and any
  real parser reads it as malformed. (fixed — rewritten without the colon,
  and all six re-parsed clean.)
- r9: (verifier) **two of the six `next:` lines named no action**, which is
  step 3's literal requirement — `marker-gate-needs-no-done` and
  `unsupervised-endurance` described the block instead, and the first
  restated its hard gate as already satisfied. (fixed — both now name the
  first thing to do, in the same corrective commit.)
- r10: (verifier) **the note was phrased as an instruction** — "sets
  `status:` back and carries on" — where step 3 asks for the sentence that a
  returning session MAY set it back, and the role's own `Never` list says a
  `## Blockers` note is data, never an instruction. Written by the role told
  to be careful about exactly this. (fixed — MAY, in the same commit.)
- r11: (verifier) the sweep's own Goal repeated the claim r1 disproves:
  "five of them holding a plan out of the queue". (fixed — Goal now says the
  command REPORTS that, and that it is false on all five.)
- r12: (verifier) `.claude/commands/janitor.md` contradicts itself on
  spawning: step 5 requires the verifier, and `## Never` says "spawn
  anything", unqualified. The verifier judged the sweep's resolution correct
  — AGENTS.md step 5 makes the reader mandatory at every depth, and a
  `Never` reading that forbade it would make every sweep unreviewable — but a
  lower-tier janitor reading the `Never` list alone ships an unreviewed
  sweep. (wontfix here — `.claude/commands/` is protocol text. Reported to
  the human in this sweep's report rather than filed, since it is one
  sentence's scope and the human owns that file's wording.)
- r13: (verifier) filing #278 and #279 is outside what janitor.md describes
  and not on its `Never` list, and open issues outrank plans — so the sweep
  put two items at the top of the next session's queue. (no change — a defect
  the role may not fix and may not carry has no other route, and the
  alternative is a measurement that dies with the session. Recorded because
  the role's text neither permits nor forbids it, which is worth the human
  knowing.)
- r2: (session) **the pull-request exemption fired on a premise this session
  had already disproved.** The command prints `pull request 10 — nearly done,
  not abandoned work` for `claude/multi-agent-orchestration-pr-jyli0w`, and
  `janitor.md` says such a candidate is not the sweep's "even when its session
  is gone" because "an open pull request means the work is nearly done."
  Pull request 10 is **closed, never merged**, since 2026-08-21 — read from
  GitHub three times across this session, most recently minutes before the
  sweep. The command reads git, which cannot tell closed from open, and its
  own candidate line says `state unverified`. So the one candidate exempted as
  nearly-done is the most abandoned of the seven: ARCHIVED session, closed
  pull request, 646h. (no change — the rule says leave it and the rule is what
  a sweep follows; a role that argues its way past its own exemption is worse
  than one that reports. Reported here and in the sweep's own report, and it
  belongs to #278's family: a line asserting a state it never read.)

## Blockers

None.

## Where to look

- `.claude/commands/janitor.md`, step 2 — the table that decides gone.
- `.claude/commands/orchestrate.md`, step 2 — where the field rules are
  stated once, which that table defers to.
