---
workstream: janitor-2026-09-17
status: in-progress
branch: claude/drain-8jr601
pr: none
plan: none
issue: none
session: https://claude.ai/code/session_01K6sHM4RWyYDCLZmrDSrWk3
agent: opus
updated: 2026-09-17
next: Read the control plane per candidate, release only what reads gone, then retire this file and open the pull request
---

## Goal

`./joharness.sh drain` and `./joharness.sh janitor` both read `janitor : DUE`
— 648h since the repository began, no sweep having landed. First sweep this
repo has had. Seven candidates, five of them holding a plan out of the queue
while their claims stand.

A claim outlives the session that made it and nothing releases it. The role
exists because of two measurements in a consumer: issue #254, one unowned
block held four plans out of the queue for 141 hours while every pass printed
it as `holds no slot`; issue #249, the mechanism that answers liveness is the
one nobody schedules.

## Decisions

- **Push age names a candidate; only the control plane names a verdict.** The
  command's own output says liveness is not in it. Every release below cites
  the reading that proved it, per the field table in
  `.claude/commands/orchestrate.md` step 2 — `ARCHIVED` or not found, or a
  FAILED bucket confirmed by a second read with `updated_at` and the head
  both unchanged. `IDLE` alone is between turns, not gone, whatever the push
  age says.
- **Two reads for anything that is not ARCHIVED or absent.** The cost of the
  safe direction is one sweep's delay; the cost of the other is destroyed
  work in progress and a duplicate session on a live branch.

## Rejected

- Nothing yet.

## Review

- (to be written from the reader's findings, before their fixes)

## Blockers

None.

## Where to look

- `.claude/commands/janitor.md`, step 2 — the table that decides gone.
- `.claude/commands/orchestrate.md`, step 2 — where the field rules are
  stated once, which that table defers to.
