---
workstream: curate-2026-09-17
status: in-progress
branch: claude/drain-8jr601
pr: none
plan: none
issue: none
session: https://claude.ai/code/session_01K6sHM4RWyYDCLZmrDSrWk3
agent: opus
updated: 2026-09-17
next: Review, then retire this file in the last commit before the pull request — the retire commit is what dates the cycle
---

## Goal

`./joharness.sh drain` and `./joharness.sh curate` both read `curate : DUE` —
10 plan files changed since the last curate, at a threshold of 10. First
curate this repository has had, alongside today's first janitor sweep.

The reader's verdict is **`NOTHING TO CURATE` — 5 free plan(s), every
declaration reads true**, 0 held by a manager. That is the common answer on a
healthy queue and the role says so in its own words.

## Decisions

- **A clean pass still lands its pull request.** `curate.md` §0.2 is explicit:
  the cycle's date is the base-branch commit that DELETES a
  `docs/handover/curate-*.md`, so a pass that lands nothing clears nothing and
  the next session is handed the identical item for ever. The role carries the
  measurement that bought the rule — this repository read `curate : DUE` on
  every pass, and under unsupervised the heartbeat re-seeds sessions that each
  curate, land nothing and re-arm the trigger.
- **Sections 2, 3 and 4 are skipped, not attempted.** §0.2 says so for a clean
  pass. There is no REPAIR line to take, no DECLUTTER signal to check against
  merged history, and PROPOSE has nothing to propose: the reader drew no
  registry candidate (3+ plans on one path) and no split candidate (8+ Scope
  bullets).
- **This is `NOTHING TO CURATE`, not `NOTHING READ`**, and the pull request
  body says which. The second means the queue is empty or every plan is
  HELD — a pass with no subject rather than a clean one. Here 5 plans were
  read and every declaration held. A reader told only "nothing" cannot tell
  those apart, which is why the role separates them.

## Rejected

- **Widening the pass to find something to do.** The queue is 5 plans and 3
  open questions and a curator could always find prose to improve. The role's
  `## Never` list is the boundary: no splitting, no reordering, no `urgency:`,
  no requirement, no research file, no protocol path — and no plan body beyond
  a stale anchor. A clean verdict is a result, not a failure to look hard
  enough.
- **Skipping the pull request because the diff is empty.** That is the exact
  failure §0.2 names, and this repository is the one it was measured in.

## Review

- (to be written from the reader's findings, before their fixes)

## Blockers

None.

**Edge work, dispositioned** (Loop step 2, which `drain` ranks above this):
`origin/claude/multi-agent-orchestration-pr-jyli0w` names pull request 10.
Read from GitHub this session: `state: closed`, `merged: false`, closed
2026-08-21. A closed, never-merged pull request is not edge work and does not
outrank the queue. Its branch is the human's to triage or delete.

## Where to look

- `.claude/commands/curate.md` §0.2 — why a clean pass still claims and
  retires, and the measurement behind it.
- `joharness.sh:cmd_curate` — the reader whose verdict this pass acted on.
