---
workstream: curate-2026-09-11
status: in-progress
branch: claude/rescope-job-description-ehcvid
pr: none
plan: none
issue: none
session: https://claude.ai/code/session_01CH8JvqV2WAada6gNuuWwuz
agent: sonnet
updated: 2026-09-11
next: Narrow the two whole-directory scope: claims to the files each plan's Scope section names
---

## Goal

`./joharness.sh drain` printed `curate : DUE — 76 plan file(s) changed since
the queue began, none having landed`. One pass over the plan queue, one pull
request, exit. The retire commit is what dates the cycle, so it lands on every
path through this role.

`./joharness.sh curate` reports 2 repairs, 0 declutter candidates, 0
proposals:

- `harness-agents-cut`: `scope:` claims the whole directory `.agents/docs`
- `orchestrated-run`: `scope:` claims the whole directory `docs/product`

## Decisions

- Branch is the session's designated one, restarted from `main` (its own
  pull request merged at `b4979b0`, so it cannot carry follow-up work). The
  identity `dispatch` keys on is this FILE — `workstream: curate-<stamp>`,
  `plan: none` — not the branch name, so the reuse costs nothing.
- `orchestrated-run`: `docs/product` narrowed to
  `docs/product/orchestrated-mode.md`. Its `## Scope` annotates one
  requirement's last bullet and its `requirement:` field names the same
  stem, so the file is nameable and the directory claim was swallowing the
  rest for nothing.
- `orchestrated-run`: `.agents/docs/orchestrated.md` ADDED to `scope:`.
  Its `## Scope` bullet 2 records the run's numbers into that file's Runs
  table — a path the plan writes and did not declare. `curate` did not flag
  it: the section reader takes only the FIRST backticked token of a bullet
  that STARTS with one (the rule separating a deliverable from a citation),
  and this bullet opens "Record in". The class is section 2's "`## Scope`
  names a path `scope:` does not cover — add it", which is the measured
  failure this whole mechanism exists for.
- NOT `shared:` on that path. One plan declares it, so a routine reconcile
  there is unprovable; marking it would claim a parallel safety no second
  declaration supports, which section 2 calls worse than claiming none.
- `harness-agents-cut`: `.agents/docs` LEFT as a whole-directory claim, the
  one REPAIR line not taken. Its `## Scope` names `.agents/docs/**` as the
  directory that RECEIVES moved rules and names no file — the plan cannot
  know which until the cut is designed. Narrowing it to any single file
  would be inventing a path that looks plausible, which section 2 forbids in
  the same breath. Section 2's own closing rule decides it: a path a plan
  genuinely edits in place stays exclusive. The line will still print on the
  next curate; it is answered, not dropped.

## Rejected

## Review

## Blockers

None.

## Where to look

- `docs/plans/harness-agents-cut.md` — `scope:` and its `## Scope` section.
- `docs/plans/orchestrated-run.md` — same.
- `.claude/commands/curate.md`, section 2 — a directory claim swallows every
  file under it and collides with every plan touching the directory for
  nothing.
